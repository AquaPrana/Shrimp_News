import { requireAdmin } from "@/lib/admin-auth";
export default async function SettingsPage(){const admin=await requireAdmin();return <div className="space-y-6"><header><p className="text-xs font-bold uppercase tracking-[0.28em] text-cyan-700">Administration</p><h1 className="mt-2 text-3xl font-extrabold text-slate-900">Settings</h1></header><section className="max-w-2xl rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="text-lg font-bold">Admin profile</h2><dl className="mt-5 grid gap-4 sm:grid-cols-2"><div><dt className="text-xs uppercase text-slate-500">Name</dt><dd className="mt-1 font-semibold">{admin.name}</dd></div><div><dt className="text-xs uppercase text-slate-500">Email</dt><dd className="mt-1 font-semibold">{admin.email}</dd></div><div><dt className="text-xs uppercase text-slate-500">Role</dt><dd className="mt-1 font-semibold capitalize">{admin.role}</dd></div></dl><p className="mt-6 rounded-xl bg-slate-50 p-4 text-sm leading-6 text-slate-600">Admin credentials are provisioned from secure server environment variables. Restart with updated values before the first login to create the initial administrator.</p></section></div>}
